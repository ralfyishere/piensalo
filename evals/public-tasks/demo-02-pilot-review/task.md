<!-- Fable Think eval task | label: DEMO | id: demo-02-pilot-review -->

# Task: Synthesize the delivery-pilot results

Two internal documents about the same 2024 route-consolidation pilot are in
`public-context/`: a field report and a methods appendix. Read BOTH fully,
then report what the pilot actually demonstrated.

Deliverable contract for your answer file:

1. A short `SUMMARY:` section in prose (informative, not contract-scored).
2. A line exactly `VERIFIED_REDUCTION_PCT: <integer percent>`
3. A line exactly `CONTRADICTION_FOUND: <yes|no>` — do the two documents
   disagree about the headline result?
4. A line exactly `CONTROLLING_SOURCE: <file name>` — which document's
   number should be treated as authoritative.

The three summary lines are parsed by a script; get the formats exact.
