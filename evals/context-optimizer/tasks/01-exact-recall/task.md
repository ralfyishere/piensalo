Using only the provided project context, report the exact staging deploy command, the pinned commit, and the currently active hosting decision (the superseded one does not count).

Quote the command and commit byte-for-byte. Answer with exactly these lines:
COMMAND: <exact command>
COMMIT: <exact commit>
HOSTING: <one sentence>
